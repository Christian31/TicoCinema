@model IEnumerable<TicoCinema.WebApplication.Models.AudienceClassification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Acronym)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Restriction)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Acronym)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Restriction)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AudienceClassificationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AudienceClassificationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AudienceClassificationId })
        </td>
    </tr>
}

</table>
