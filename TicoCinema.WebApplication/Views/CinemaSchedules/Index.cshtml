@model IEnumerable<TicoCinema.WebApplication.Models.CinemaSchedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BeginDatetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinishDatetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cinema.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MovieFormat.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BeginDatetime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinishDatetime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cinema.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MovieFormat.Name)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.CinemaScheduleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CinemaScheduleId })
        </td>
    </tr>
}

</table>
